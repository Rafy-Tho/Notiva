# NoteFlow — Deployment

## Hosting Platform

**Inferred:** Render.com (based on README URL: `https://noteflow.rafytho.com/`)

**Note:** No explicit deployment configuration files (Dockerfile, Docker Compose) were found in the repository.

---

## Frontend Deployment

### Build Process

```bash
npm run build
```

**Output:** `frontend/dist/` directory (static files)

**Deployment:**

- Static file hosting (likely served via CDN)
- Entry point: `index.html`
- Bundled assets generated by Vite

### Runtime Configuration

| Variable        | Purpose         |
| --------------- | --------------- |
| `VITE_BASE_API` | Backend API URL |

---

## Backend Deployment

### Build Process

No build step required (native ESM, no transpilation). The Prisma client is generated automatically:

- `npm start` runs `prisma generate` before booting (`"start": "prisma generate && node src/server.js"`), so generation is guaranteed on every start.
- A `postinstall` script also runs `prisma generate` after `npm install`/`npm ci`.

### Hostinger (cPanel Node.js)

There is no separate build command in the Hostinger panel. Set the application start script to **`npm start`**; the `start` script above regenerates the Prisma client before the server boots. Ensure all required env vars (`DATABASE_URL`, `JWT_ACCESS_SECRET`, `CLOUDINARY_*`, `HOSTINGER_MAIL_*`, `MAIL_FROM`, `MAIL_FROM_NAME`) are set in the Node.js env panel **before the first start** — `prisma generate` fails if it cannot resolve `DATABASE_URL` (loaded via `backend/prisma.config.js`).

### Other platforms (e.g. Render)

With a separate build step (e.g. Render), set the **Build Command** to:

```bash
npm install && npx prisma generate
```

and the **Start Command** to `npm start`. If the environment previously cached a stale Prisma client, use **Clear build cache** before redeploying.

### Runtime Configuration

| Variable            | Purpose                        | Required |
| ------------------- | ------------------------------ | -------- |
| `DATABASE_URL`      | PostgreSQL connection string   | Yes      |
| `JWT_ACCESS_SECRET` | JWT signing secret             | Yes      |
| `PORT`              | Server port                    | Yes      |
| `FRONTEND_ORIGIN`   | CORS allowed origins           | Yes      |
| `JWT_TTL`           | Token expiration (default: 7d) | No       |
| `CLOUDINARY_*`      | Cloudinary credentials         | No       |
| `BREVO_*`           | Email service credentials      | No       |
| `NODE_ENV`          | Environment mode               | No       |

**Startup:**

```bash
npm start
# Or for development:
npm run dev
```

---

## Database Deployment

**PostgreSQL** (cloud-hosted, e.g. Neon/RDS)

**Configuration:**

- Connection string via `DATABASE_URL`
- Network access configured to allow backend IP
- Migrations applied with `npx prisma migrate deploy`

---

## External Services

| Service    | Deployment Requirement                    |
| ---------- | ----------------------------------------- |
| PostgreSQL | Provision cloud database + run migrations |
| Cloudinary | Create account, get credentials           |
| Brevo      | Create account, get API key               |

---

## Environment Configuration

### Development

```
NODE_ENV=development
PORT=5000
FRONTEND_ORIGIN=http://localhost:5173
```

### Production

```
NODE_ENV=production
PORT=<assigned by host>
FRONTEND_ORIGIN=https://noteflow.rafytho.com/
```

---

## Deployment Dependencies

| Dependency   | Purpose         |
| ------------ | --------------- |
| Node.js v18+ | Runtime         |
| npm          | Package manager |

---

## Startup Process

```
1. Load environment variables (dotenv)
2. Connect to PostgreSQL (Prisma `$connect`)
3. Initialize Express app
4. Set up middleware (helmet, cors, etc.)
5. Register routes
6. Listen on PORT
```

---

## Configuration Differences

| Aspect      | Development                           | Production              |
| ----------- | ------------------------------------- | ----------------------- |
| Logging     | Custom logger + httpLogger (detailed) | Custom logger (minimal) |
| Error stack | Included                              | Excluded                |
| CORS        | Localhost                             | Specific domain         |
| Database    | Atlas (dev/prod)                      | Atlas (prod)            |
| Cookies     | secure=false (dev)                    | secure=true (prod)      |

---

## Build Dependencies

| Tool     | Version |
| -------- | ------- |
| Vite     | 8       |
| ESLint   | 10      |
| Prettier | Latest  |
